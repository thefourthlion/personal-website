const express = require("express"); const router = express.Router(); const { createContact, readContact, readContactFromID, updateContact, deleteContact, } = require("../controllers/Contact"); router.route("/create").post(createContact); router.route("/read").get(readContact); router.route("/read/:id").get(readContactFromID); router.route("/update/:id").post(updateContact); router.route("/delete/:id").delete(deleteContact); module.exports = router; 