const mongoose = require("mongoose"); const journalSchema = new mongoose.Schema( {header: { type: String, required: [true, "Please provide header"],  },entry: { type: String, required: [true, "Please provide entry"],  },timestamp: { type: String, required: [true, "Please provide timestamp"],  }, }, { timestamps: true } ); const journal = mongoose.model("journal", journalSchema); module.exports = journal;